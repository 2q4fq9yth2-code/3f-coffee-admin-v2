# RULES.md — 不可协商规则

1. 不直接向 `main` 提交。
2. 没有测试契约，不编码。
3. 没有自动测试或明确人工验收证据，不合并。
4. 金额、订单、库存、会员余额改动必须包含失败场景测试。
5. 禁止虚构业务数据或把 Mock 数据包装成真实数据。
6. 用户端 MVP 固定为 Home / Mood / Order / Profile；新增页面必须单独决策。
7. 当前仓库优先服务 June 后台低负担操作，不做大而全 ERP。
8. 一个 PR 只解决一个主题；超过 300 行净新增需解释，超过 500 行默认拆分。
9. PR 必须包含：契约、测试证据、风险、回滚、截图或接口样例。
10. CI 不通过、未解决 review thread、存在高风险未说明项，禁止合并。
11. 数据库破坏性迁移必须先备份、提供回滚 SQL，并分阶段发布。
12. 任何“成功”状态必须来自真实后端确认；不允许前端自我宣布成功。
