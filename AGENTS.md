# AGENTS.md — THIRD FLOOR / June 私人研发规则

## 1. 项目身份

本项目服务于 June 的 Third Floor 实体业务。它不是练习项目，也不是为了展示技术复杂度。

长期定位：Coffee + Community + Business。
当前应用 MVP 已冻结为 4 个用户页：Home / Mood / Order / Profile。
核心闭环：情绪输入 → 咖啡推荐 → 下单 → 记录状态。
当前仓库 `3f-coffee-admin-v2` 的职责是 June 的低负担运营后台，不得反向扩大用户端 MVP。

## 2. June 的工作方式

- June 是业务决策人，不要求她承担复杂的技术判断。
- 输出先给：1 个结论、最多 3 个关键点、1 个下一步。
- 不用长篇概念解释代替可执行结果。
- 遇到缺失信息，先读取项目契约、现有代码和数据；仍无法确认时，使用最保守实现并在 PR 标注假设。
- 禁止虚构价格、成本、库存、销量、会员余额、营业状态或业务效果。

## 3. TCD 强制流程

任何功能、Bug 修复、数据结构调整、UI 重构都必须按顺序执行：

1. 阅读 `project-brain/` 与相关 `contracts/`。
2. 创建或更新测试契约，写清验收条件与失败场景。
3. 先写失败测试，或明确无法自动化的人工验收步骤。
4. 只实现契约要求的最小改动。
5. 运行 `npm run check`。
6. 通过 PR 提交，不直接向 `main` 推送。
7. PR 中必须写风险、回滚方案、截图/证据和未覆盖项。

没有契约，不开始编码；契约未通过，不允许合并。

## 4. 产品边界

### 用户端仅保留

- Home：门店状态、今日推荐、进入 Mood / Order。
- Mood：选择当前情绪，得到可解释的咖啡推荐。
- Order：确认商品、规格、自取信息，提交订单或进入真实支付/联系路径。
- Profile：查看本人的订单和情绪记录，不做复杂社交关系。

### 当前不做

- 完整社交关系链、动态广场。
- 复杂积分商城与成长等级。
- 多商户、多门店平台。
- 复杂知识付费系统。
- 为了“看起来高级”而增加不产生业务结果的页面。

### June 后台优先级

1. 快速修改营业状态。
2. 快速修改菜单价格、上下架和售罄状态。
3. 查看和处理订单。
4. 查看活动报名与基础经营数据。

任何后台操作必须以一人店低操作成本为第一标准。

## 5. 业务数据规则

- 金额使用整数分或明确的 Decimal，禁止二进制浮点直接参与结算。
- 菜单价格、库存、售罄状态必须来自唯一数据源，禁止前端写死后与后台分叉。
- 订单必须具备幂等键，重复点击或网络重试不得产生重复订单。
- 会员余额、优惠券、退款属于高风险数据，任何修改必须有审计字段。
- 营业时间默认业务事实为 07:00–22:00，但页面展示必须读取后台配置，不得硬编码。
- 未接入真实支付前，不得把“提交成功”描述为“支付成功”。

## 6. 咖啡店专属约束

- 现阶段不加入冷萃和 Dirty。
- 三明治为严格两层结构：两片吐司 + 单层夹心；不得设计成三层堆叠。
- 菜单与推荐优先适配低人工、低耗材、低损耗、一人店效率。
- 用户端推荐必须允许用户跳过情绪推荐，直接查看完整菜单。
- AI 推荐只负责建议，不得自动替用户下单或修改订单。

## 7. UI 与可访问性

- 移动端优先；最低覆盖 375×667 与 390×844。
- 风格：Ultra Minimal / Premium / Editorial / Modern SaaS，但保留真实咖啡店温度。
- 不允许为了装饰牺牲可读性；正文至少 14px，触控区域至少 44×44px。
- 所有按钮必须有明确状态：默认、加载、成功、失败、禁用。
- 重要操作不得仅靠颜色表达。
- 任何页面只有一个主要行动目标。

## 8. 代码规则

- 保持现有 Vue 3 + Vite + Pinia + Element Plus 技术栈。
- 未经契约批准，不得迁移框架、状态库或 UI 库。
- 优先小文件、小组件、小函数；一个 PR 只解决一个可描述问题。
- 新增 API 必须有请求、响应、错误和幂等契约。
- 禁止提交 `.env`、密钥、真实用户隐私数据或生产数据库快照。
- 禁止用假成功、静默 catch 或硬编码 mock 掩盖接口失败。

## 9. Agent 角色

- Architect：维护边界、数据流和契约，不直接扩大范围。
- Builder：只实现已批准契约，不顺手重构无关模块。
- Tester：先找失败路径、重复提交、金额和状态问题。
- Reviewer：以业务风险、回归、可维护性为主，不只看代码风格。
- Release Keeper：确认环境变量、数据库变更、备份和回滚。

同一个 Agent 不得在没有独立复核的情况下同时宣称“实现完成”和“验收通过”。
